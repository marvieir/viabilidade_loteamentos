# Backend — Pré-Viabilidade de Loteamento (FastAPI)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
shapely==2.0.6
pyproj==3.7.0
lxml==5.3.0
rasterio==1.4.3  # Fase 2.2 — amostragem do raster de vegetação (área verde)
pyogrio==0.13.0  # Fase 13 — leitura vetorial por JANELA (bbox) das camadas ambientais (escala estado/BR)
python-dotenv==1.0.1  # carrega backend/.env via `uvicorn --env-file .env`
anthropic==0.69.0  # Fase 1.8 — extrator real da LUOS (import TARDIO, gated por ANTHROPIC_API_KEY; testes não usam)
google-genai==1.21.0  # Fase 11.14 — fallback do gerador de programa (Gemini; import TARDIO, gated por GOOGLE_API_KEY; testes não usam)
fpdf2==2.8.7  # Fase 7 — laudo de triagem em PDF (Python puro, offline, sem fonte externa nem rede)

# Fase 12 — multi-tenant (banco + autenticação)
SQLAlchemy==2.0.36        # ORM (modelos usuarios/analises)
alembic==1.14.0           # migrações versionadas (schema estável)
psycopg[binary]==3.2.3    # driver Postgres (produção); SQLite no dev/teste é nativo
PyJWT==2.10.1             # tokens de sessão (access/refresh)
bcrypt==4.2.1             # hash de senha (usado direto, sem passlib)

# dev / testes
pytest==8.3.4
httpx==0.28.1
